{
  "name": "anvil-lsp-vscode-extension",
  "version": "0.0.0-development",
  "description": "Anvil Language Server VS Code Extension",
  "author": "Wern Lim <dev@wern.cc>",
  "license": "MIT",
  "main": "./client/out/extension",
  "categories": [],
  "keywords": [
    "multi-root ready"
  ],
  "engines": {
    "vscode": "^1.100.0"
  },
  "contributes": {
    "languages": [
      {
        "id": "anvil",
        "aliases": [
          "Anvil",
          "anvil",
          "AnvilHDL",
          "Anvil HDL",
          "anvil-hdl"
        ],
        "extensions": [
          ".anvil"
        ],
        "configuration": "./anvil.configuration.json"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Anvil Language Server Configuration",
      "properties": {
        "anvil.maxNumberOfProblems": {
          "scope": "resource",
          "type": "number",
          "default": 100,
          "description": "Controls the maximum number of problems produced by the server."
        },
        "anvil.projectRoot": {
          "scope": "resource",
          "type": "string",
          "default": "",
          "description": "Controls the project root path. Defaults to the workspace root."
        },
        "anvil.executablePath": {
          "scope": "resource",
          "type": "string",
          "default": "",
          "description": "Path to the Anvil compiler binary. Defaults to 'anvil' in your PATH."
        },
        "anvil.showTimingInfo": {
          "scope": "window",
          "default": { "asInlayHints": "condensed", "onHover": true },
          "description": "Controls how clock-cycle timing and expression lifetime information is shown in the editor.",
          "oneOf": [
            {
              "type": "boolean",
              "description": "`true` enables timing info as condensed inlay hints and on hover. `false` disables all timing info."
            },
            {
              "type": "object",
              "description": "Fine-grained control over where clock-cycle timing and lifetime information appears.",
              "properties": {
                "asInlayHints": {
                  "default": "condensed",
                  "description": "Show clock-cycle timings and expression lifetimes as inline annotations in the editor gutter.",
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": ["none", "condensed", "full"],
                      "enumDescriptions": [
                        "Do not show timing info as inlay hints.",
                        "Show timing info as compact prefixed and postfixed inlay hints.",
                        "Show timing info in full phrases as prefixed and postfixed inlay hints."
                      ]
                    },
                    {
                      "type": "boolean",
                      "description": "`true` is equivalent to `\"condensed\"`. `false` is equivalent to `\"none\"`."
                    }
                  ]
                },
                "onHover": {
                  "type": "boolean",
                  "default": true,
                  "description": "Show clock-cycle timing and lifetime information when hovering over an expression."
                },
                "onAutocomplete": {
                  "type": "boolean",
                  "default": false,
                  "description": "Show clock-cycle timing and lifetime information in autocomplete documentation popups."
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "anvil.showSyntaxHelp": {
          "scope": "window",
          "default": { "onHover": true, "onAutocomplete": "anvilKeywords", "includeExamples": false },
          "description": "Controls whether explanations of Anvil syntax are shown in hover tooltips and autocomplete popups.",
          "oneOf": [
            {
              "type": "boolean",
              "description": "`true` enables all explanations in all areas. `false` disables all explanations."
            },
            {
              "type": "object",
              "description": "Fine-grained control over where Anvil concept explanations appear.",
              "properties": {
                "onHover": {
                  "type": "boolean",
                  "default": true,
                  "description": "Show Anvil explanations when hovering over nodes."
                },
                "onAutocomplete": {
                  "default": "anvilKeywords",
                  "description": "Show Anvil explanations in autocomplete documentation popups.",
                  "oneOf": [
                    {
                      "type": "string",
                      "enum": ["none", "anvilKeywords", "all"],
                      "enumDescriptions": [
                        "Never show explanations in autocomplete popups.",
                        "Show explanations only for built-in Anvil keywords and operators.",
                        "Show explanations for all completions, including user-defined identifiers."
                      ]
                    },
                    {
                      "type": "boolean",
                      "description": "`true` is equivalent to `\"all\"`. `false` is equivalent to `\"none\"`."
                    }
                  ]
                },
                "includeExamples": {
                  "type": "boolean",
                  "default": false,
                  "description": "When `true`, code examples from the Anvil language reference are included in syntax help explanations. Only applies when syntax explanations are active."
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "anvil.trace.server": {
          "scope": "window",
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Traces the communication between VS Code and the language server."
        }
      }
    },
    "grammars": [
      {
        "language": "anvil",
        "scopeName": "source.anvil",
        "path": "./grammar.json"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "tsc -b",
    "build:client": "cd client && tsc -b",
    "build:server": "cd server && npm run build",
    "watch": "tsc -b -w",
    "lint": "eslint",
    "postinstall": "cd client && npm install && cd ../server && npm install && cd .."
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@stylistic/eslint-plugin": "^2.9.0",
    "@types/node": "^22",
    "eslint": "^9.13.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.39.0"
  }
}
