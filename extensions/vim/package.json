{
  "name": "anvil-coc-nvim-extension",
  "version": "0.0.0-development",
  "description": "Anvil Language Server coc.nvim Extension",
  "author": "Wern Lim <dev@wern.cc>",
  "license": "MIT",
  "main": "./out/index.js",
  "keywords": [
    "coc.nvim"
  ],
  "engines": {
    "coc": "^0.0.82"
  },
  "scripts": {
    "watch": "concurrently 'node esbuild.mjs --watch' 'cd server && npm run watch -- --preserveWatchOutput'",
    "build:client": "node esbuild.mjs",
    "build:server": "cd server && npm run build",
    "build": "npm run build:client && npm run build:server",
    "prepare": "node esbuild.mjs",
    "postinstall": "cd ./server && npm install"
  },
  "devDependencies": {
    "coc.nvim": "^0.0.83-next.18",
    "esbuild": "^0.25.11",
    "concurrently": "^7.6.0",
    "typescript": "^5.3.3"
  },
  "activationEvents": [
    "*"
  ],
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "coc-anvil configuration",
        "properties": {
          "anvil.maxNumberOfProblems": {
            "scope": "resource",
            "type": "number",
            "default": 100,
            "description": "Controls the maximum number of problems produced by the server."
          },
          "anvil.projectRoot": {
            "scope": "resource",
            "type": "string",
            "default": "",
            "description": "Controls the project root path. Defaults to the workspace root."
          },
          "anvil.executablePath": {
            "scope": "resource",
            "type": "string",
            "default": "",
            "description": "Path to the Anvil compiler binary. Defaults to 'anvil' in your PATH."
          },
          "anvil.inlayHints.timingInfo": {
            "scope": "window",
            "type": "boolean",
            "default": true,
            "description": "Controls whether inlay hints denoting event timings are shown in the editor."
          },
          "anvil.showSyntaxHelp": {
            "scope": "window",
            "default": { "onHover": true, "onAutocomplete": "anvilKeywords", "includeExamples": false },
            "description": "Controls whether explanations of Anvil syntax are shown in hover tooltips and autocomplete popups.",
            "oneOf": [
              {
                "type": "boolean",
                "description": "`true` enables explanations on hover and for built-in Anvil keyword completions. `false` disables all explanations."
              },
              {
                "type": "object",
                "description": "Fine-grained control over where Anvil concept explanations appear.",
                "properties": {
                  "onHover": {
                    "type": "boolean",
                    "default": true,
                    "description": "Show Anvil concept explanations when hovering over nodes."
                  },
                  "onAutocomplete": {
                    "default": "anvilKeywords",
                    "description": "Show Anvil concept explanations in autocomplete documentation popups.",
                    "oneOf": [
                      {
                        "type": "string",
                        "enum": ["none", "anvilKeywords", "all"],
                        "enumDescriptions": [
                          "Never show explanations in autocomplete popups.",
                          "Show explanations only for built-in Anvil keywords and operators.",
                          "Show explanations for all completions, including user-defined identifiers."
                        ]
                      },
                      {
                        "type": "boolean",
                        "description": "`true` is equivalent to `\"all\"`. `false` is equivalent to `\"none\"`."
                      }
                    ]
                  },
                  "includeExamples": {
                    "type": "boolean",
                    "default": false,
                    "description": "When `true`, code examples from the Anvil language reference are included in syntax help explanations. Only applies when syntax explanations are active (`onHover` or `onAutocomplete`)."
                  }
                },
                "additionalProperties": false
              }
            ]
          }
        }
    }
  }
}
