{
  "name": "anvil-coc-nvim-extension",
  "version": "0.0.0-development",
  "description": "Anvil Language Server coc.nvim Extension",
  "author": "Wern Lim <dev@wern.cc>",
  "license": "MIT",
  "main": "./out/index.js",
  "keywords": [
    "coc.nvim"
  ],
  "engines": {
    "coc": "^0.0.82"
  },
  "scripts": {
    "watch": "concurrently 'node esbuild.mjs --watch' 'cd server && npm run watch -- --preserveWatchOutput'",
    "build:client": "node esbuild.mjs",
    "build:server": "cd server && npm run build",
    "build": "npm run build:client && npm run build:server",
    "prepare": "node esbuild.mjs",
    "postinstall": "cd ./server && npm install"
  },
  "devDependencies": {
    "coc.nvim": "^0.0.83-next.18",
    "esbuild": "^0.25.11",
    "concurrently": "^7.6.0",
    "typescript": "^5.3.3"
  },
  "activationEvents": [
    "*"
  ],
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "coc-anvil configuration",
        "properties": {
          "anvil.maxNumberOfProblems": {
            "scope": "resource",
            "type": "number",
            "default": 100,
            "description": "Controls the maximum number of problems produced by the server."
          },
          "anvil.projectRoot": {
            "scope": "resource",
            "type": "string",
            "default": "",
            "description": "Controls the project root path. Defaults to the workspace root."
          },
          "anvil.executablePath": {
            "scope": "resource",
            "type": "string",
            "default": "",
            "description": "Path to the Anvil compiler binary. Defaults to 'anvil' in your PATH."
          },
          "anvil.inlayHints.timingInfo": {
            "scope": "window",
            "type": "boolean",
            "default": true,
            "description": "Controls whether inlay hints denoting event timings are shown in the editor."
          }
        }
    }
  }
}
